# FLiK — Fix Swarm Progress Report

**Started**: 2026-05-21
**Source**: `docs/SATISFACTION_REPORT.md` (20-domain audit)
**Strategy**: 10 parallel agents addressing 10 RELEASE BLOCKERS + 15 INACTIVE features + 15 NEEDS IMPROVEMENT items grouped by file ownership to avoid conflict.

---

## Fix swarm allocation

| Agent | Focus | Issues addressed |
|---|---|---|
| **FIX #1** | Payment stack | `composer require midtrans/midtrans-php`, admin gift/referral routes+views, 6 missing gift/referral views, scheduler crons, header link |
| **FIX #2** | DRM pipeline | Align method signatures across TranscodingPipeline/Ffmpeg/HlsSegmenter/HlsEncryptor/UploadToBunny, add `/drm/segment` + `/drm/playlist` routes, wire ForensicWatermarker into PlaybackManifestGenerator, enforce DRM on EpisodeWatchController, apply GeoBlock middleware to playback, install firebase/php-jwt + geoip2 |
| **FIX #3** | Analytics surface | Funnel controller method rename, Cohort export method, full A/B suite alignment (schema↔model↔service↔view), smoke tests |
| **FIX #4** | Missing views | blog/{show,category,rss}.blade.php, watch-party/{create,join}.blade.php, WatchPartyController::chat method rename, blog header nav link, BlogSeeder fix |
| **FIX #5** | Feature Flags + Settings admin UI | Routes + admin/feature-flags/* views + admin/settings/* views, sidebar surface verify, smoke tests |
| **FIX #6** | Auth hardening | 2FA UI link + middleware apply, OAuth account-linking by email, session regenerate, toggleAdmin audit + self-revocation guard, MenuMatrixController column/pivot fix, remove insecure storeMovie upload path |
| **FIX #7** | AI activation | Wire ColdStartRecommender into RecommendationEngine, post-register onboarding redirect, mount `<x-movie-seo>` on detail, AnalyzeCommentSentiment listener, taskType labels on 13 untracked tasks, ThumbnailPicker → UsageTracker, decide Soundtrack |
| **FIX #8** | Silent bugs | Trending recency boost (Carbon 3 fix), SecurityAlertService severity map to SecurityEvents constants, consolidate streak systems, Honeypot + WAF use SecurityEvents constants, SetLocale priority chain, watchlists unique index, quiz throttle |
| **FIX #9** | X-Ray data + ops gaps | SceneActorExtractor AI task + flik:ai:scene-actors command + cron, flik:schedule:remind cron registration, narrow WAF allowlist, audit retention prune, PII export include phone/address, PII_PEPPER via config |
| **FIX #10** | PWA + tests + polish | Generate 8 PWA icons + maskable + 7 iOS splash screens, viewport-fit=cover, lang-switcher in mobile-nav, PWA install track peppered hash, write 20+ critical feature tests (auth/payment/DRM/admin smoke/notif) |

---

## Status (auto-updated by main thread after each agent)

| # | Agent | Status | Notes |
|---|---|---|---|
| 1 | Payment stack | 🟢 completed | midtrans/midtrans-php ^2.6 added to composer; 3 admin routes (gifts.index, referrals.index, referrals.report) under can:admin; 7 views shipped (gift/{buy,checkout,redeem}, referrals/dashboard, admin/{gifts,referrals}/index, admin/referrals/report, emails/gift-received); header dropdown links desktop+mobile guarded by Route::has(); webhook signature abort_unless(isVerified, 403) added; scheduler entries skipped — gift/referral expire commands don't exist (flagged). |
| 2 | DRM pipeline | 🟢 completed | TranscodingPipeline rewritten to call the actual `FfmpegTranscoder::transcode($input, RenditionSpec, $output)` and `HlsSegmenter::segment($input, 6, $outputDir)` signatures (positional, segment_duration=6s, returns segment paths — manifest is always `$hlsDir/playlist.m3u8`); rendition specs serialised before persisting so RenditionSpec VOs land as arrays for downstream jobs (audit §2.1). EncryptHlsSegments now calls `HlsEncryptor::encryptSegments($hlsDir, $contentKey, $keyUrl)` (audit §2.2); a single AES-128 content key is generated per encode via DrmKeyService, encrypted via Crypt::encryptString, and stamped on `movies.drm_config.content_key_encrypted` so PlaybackController can hydrate per-user DrmSessions with the same 16 bytes. UploadToBunny replaces non-existent `uploadDirectory()` with a new `BunnyStorageService::uploadDirectory($localDir, $remotePrefix): int` that recursively walks with RecursiveDirectoryIterator (skip key.bin / enc.keyinfo), streams uploads with correct Content-Type per extension (.m3u8 → application/vnd.apple.mpegurl, .ts → video/mp2t); `encoding_renditions` payload now uses `video_bitrate` (converted to bps) instead of the bogus `bitrate` key (audit §2.3 secondary). `BunnyStorageService` no longer throws when Bunny env keys are missing — sets `$enabled=false`, logs warning once, all I/O methods short-circuit to false/null/empty/0 (audit §4.4) + new `enabled()` accessor. New routes `/drm/playlist/{movie:slug}/{rendition}.m3u8` + `/drm/segment/{movie:slug}/{rendition}/{filename}` both gated by `signed` + `geoblock` middleware, served by new `PlaybackController::playlist` + `::segment` methods that 302 to signed Bunny URLs when cdn_disk=bunny else stream from local hls_dir with path-traversal guard (audit §2.4). PlaybackManifestGenerator rewires `mediaPlaylistUrl()` + `segmentUrl()` to `URL::temporarySignedRoute` so the new routes' signed middleware revalidates each fetch; ForensicWatermarker reference wired into the master manifest via `#EXT-X-SESSION-DATA:DATA-ID="com.flik.forensic"` carrying `u{userId}|s{token8}|t{ts}`, logged to `drm` log channel for forensic trace (audit §3.2). `geoblock` middleware mounted on playback.config / playback.manifest / playback.heartbeat (audit §3.1). PlaybackController::resolveCountry() fixed to use real `App\Services\Geo\GeoIpResolver` namespace (audit §4.7). EpisodeWatchController rewritten to DI ConcurrentStreamLimiter + DrmKeyService + DrmTokenService + PlaybackManifestGenerator + GeoIpResolver — enforces inline geo gate (movie's geo_allow), acquires concurrent-stream lock against parent movie_id, mints DrmSession + JWT, exposes `$drmBundle` to view; episodes/watch.blade.php now refuses raw mp4 fallback (config-gated `drm.allow_episode_raw_mp4`) and renders Shaka via FlikPlayer when HLS available (audit §5). AdminController::storeMovie/updateMovie video upload paths replaced with redirect to new `/admin/movies/{movie}/upload` page (MovieUploadController::showUploadPage method added) + admin form view updated with status-aware notice + "Upload Master Video" button (audit §6). composer.json: added `firebase/php-jwt ^6.10` + `geoip2/geoip2 ^3.0` (audit §3.3). Movie model gets `drm_config`/`encoding_renditions`/`geo_allow` as `array` casts so all downstream code reads them as native arrays. |
| 3 | Analytics surface | 🟢 completed | **Funnel (`/admin/funnel`)**: added `FunnelAnalyzer::engagementFunnel()` as an alias that delegates to `signupToSubscribed()` so the existing controller call site keeps working; `annotateRatios()` now emits BOTH canonical (`conversion_pct` / `from_top_pct`) and legacy (`percent_from_previous` / `percent_from_top`) keys so the view + drop-off alert loop resolve correctly. **Cohort export (`/admin/cohorts/export.csv`)**: added public `CohortDashboardController::export(Request, CohortAnalyzer, AuditLogger)` that reuses the cached matrix, audit-logs `analytics.cohorts.exported`, and streams via the existing `exportCsv()` helper. **A/B suite — schema**: two new migrations `2026_05_10_240001_extend_ab_experiments_table` (slug UNIQUE backfilled from name, hypothesis, winner_variant) and `2026_05_10_240002_extend_ab_assignments_table` (assigned_at backfilled from created_at, conversion_value DECIMAL(12,2)) — both idempotent with `Schema::hasColumn()` guards. **AbExperiment model**: added `STATUS_RUNNING` constant (alias of active), `RUNNING_STATUSES`, `isRunning()` accepting either active/running, `variantKeys()` handling both flat `["a","b"]` AND object `[{key,weight}]` shapes; `normalizedVariants()` extended to detect object shape and honor embedded weights; fillable + property docblock cover slug/hypothesis/winner_variant. **AbAssignment model**: `assigned_at` / `conversion_value` added to fillable + casts (`decimal:2`); converted now flipped on `track()`. **AbTestFramework**: writes to `ab_experiment_id` (was `experiment_id`); `report()` now emits per-variant `weight` (from `normalizedVariants`), `conversions` alias of `converted`, and a top-level `significance` payload (Pearson Chi-square 2×2 for first two variants with traffic, df=1 α=0.05 critical 3.841, returns null when expected counts < 5). **AbTestController**: validates `name` UNIQUE; persists `traffic_split` (derived from object variants) + `primary_metric` (default `'conversion'`) so the NOT-NULL schema column is satisfied; `pause` action uses `isRunning()`. **Views**: `ab/show.blade.php` no longer double-multiplies `conversion_rate * 100` (service already returns 0..100), reads `converted` with `conversions` fallback, renders `weight` as `% of traffic`, uses status constants; `ab/index.blade.php` uses status constants + `variantKeys()` for count; `funnel/dashboard.blade.php` already reads the new alias keys. **Tests**: `tests/Feature/Analytics/{FunnelDashboardTest,CohortExportTest,AbTestCrudTest}.php` (RefreshDatabase, skip-on-no-DB, super_admin authz, full A/B CRUD + lifecycle + framework assign/track/report shape). All 9 changed PHP files lint clean. |
| 4 | Missing views | 🟢 completed | 5 blade views + BlogSeeder + nav link + WatchPartyController::safeBroadcastEvent shipped |
| 5 | Feature Flags + Settings | 🟢 completed | 9 admin routes registered in `routes/web.php` under the existing `admin.` prefix/name group (6 feature-flags CRUD + 3 settings CRUD/restore); each gated on `can:system.feature_flags` / `can:system.settings` per RolePermissionSeeder. URL keeps the prettier `{flag}` segment via explicit `Route::bind('flag', fn → FeatureFlag::findOrFail)` so controller signatures stay `FeatureFlag $featureFlag`. Added missing `FeatureFlagController::create()` action (only `index/store/edit/update/destroy` existed). 4 Blade views: `admin/feature-flags/{index,create,edit}.blade.php` + `admin/settings/index.blade.php` — all `<x-admin.layout>` gold theme, Alpine for tabs/strategy-picker/secret-reveal, color-coded strategy badges (off/on/role/percentage/users/authed/guests), inline POST master-switch toggle on the list, percentage slider with live preview on edit, "Roll back to 0%" emergency button, helper-usage callouts (`feature('key')` / `setting('key')`), tabbed settings UI by `group` column, per-type input rendering (bool toggle, int/float number, json/array monospace textarea, secret masked dots with reveal toggle), "Restore defaults" destructive button. Sidebar `config/admin_menu.php` entries already existed under System category — `Route::has()` filter now resolves both, so the menu items finally surface. 12 smoke tests in `tests/Feature/System/{FeatureFlagAdminTest,SettingsAdminTest}.php` covering full CRUD + 403 on non-admin + route-name reachability + Setting::get cache-bust read-back; skip cleanly via `setUpTraits()` override when DB unreachable so they don't error out before `RefreshDatabase` fires (12/12 SKIPPED in current sandbox; will all run when MySQL is provisioned). `php artisan route:list` confirms 9 new routes. |
| 6 | Auth hardening | 🟢 completed | 2FA UI wired into Security card (status badge + setup/disable modal). Admin route group now `[auth, 2fa, can:admin]` — middleware is a no-op for non-2FA users so existing admins keep working; admins-MUST-enable-2FA policy noted in route comments. LoginController rewritten: try/catch Socialite, lookup-by-email account linking, OAuth bypass closed via `hasTwoFactorEnabled()` gate + `2fa.passed`, role-aware redirect via `adminDashboardUrl()`. SessionsController::destroy now `invalidate()` + `regenerateToken()`. RegisterController calls `session()->regenerate()` + `2fa.passed`. TwoFactorController::verify regenerates BEFORE setting `2fa.passed`. AdminController::toggleAdmin now `abort_if` self-revocation + emits `SecurityEvents::ADMIN_ACTION` audit (writes via forceFill since `is_admin` is not fillable). MenuMatrixController rewritten against real schema (`roles.name` + `permission_role` pivot); heuristic fallback removed; per-render permission memo for cheap renders; view "Edit role" link now uses `roleIds[name]` for proper id-based binding. |
| 7 | AI activation | 🟢 completed | ColdStartRecommender wired into RecommendationEngine::coldStartFallback (delegates when UserPreference has signals; bridges slug→genre/era; persists batch via shared persistColdStart helper). User::preferences() hasOne relation added. RegisterController::store redirects fresh users to onboarding.quiz when preferences()->exists() is false (sets session flag onboarding.pending). EmailVerificationController::verify now routes through postVerifyRedirect helper that pushes unboarded users to /onboarding. OnboardingController::submit clears the session flag + busts the per-user rec cache; new dismiss() action + POST /onboarding/dismiss route lets the home banner suppress itself. main.blade.php gets a dismissible nudge banner (Alpine x-data, gold gradient). layout.blade.php adds @stack('head'); movie show.blade.php @push('head') mounts <x-movie-seo> so AI-generated SEO/OG/Twitter tags reach the head. EventServiceProvider boot() dispatches AnalyzeCommentSentiment on Comment::created. New flik:ai:sentiment-backfill command (--limit/--sync/--redo) walks NULL-sentiment comments in 40-chunks (analyzer's BULK_CHUNK_SIZE). SoundtrackAnalyzer: new migration 2026_05_10_240003 adds soundtrack_analysis JSON + soundtrack_analyzed_at; AnalyzeSoundtrack queueable job (ai-batch) persists via update-without-events; Admin\MovieAiController::soundtrack POST dispatches it; admin/movies/index.blade.php row dropdown gets "Soundtrack Analysis" button gated by Route::has(); <x-movies.soundtrack-analysis> collapsible component renders on detail when column populated. taskType labels + subject: added to all Wave-1 services that had defaulted to chat.generic — MovieTagger (tag.movie), SynopsisGenerator (synopsis.generate), MovieSummaryGenerator (summary.short_generate), QuoteExtractor (quote.extract), TriviaGenerator (trivia.generate), MovieReviewer (review.{perspective}), CommentModerator (comment.moderate), CommentSentimentAnalyzer (comment.sentiment_single + comment.sentiment_bulk), SeoMetaGenerator (seo.meta_generate), PromoBannerGenerator (marketing.banner_{tone}), SocialMediaPostGenerator (marketing.social_{platform}), EmailPersonalizer (email.subject_{intent} + email.body_{intent}), DailyAdminReportGenerator (admin.daily_report), TrailerSuggester (trailer.scene_score), SoundtrackAnalyzer (soundtrack.analyze), ColdStartRecommender (recommend.cold_start), RecommendationEngine.aiRerank (recommend.ai_rerank). ThumbnailPicker injects UsageTracker (nullable + app() fallback), writes vision.thumbnail_pick rows on success (uses usageMetadata.{promptTokenCount,candidatesTokenCount} or estimate 258 in + length/4 out) AND on failure (HTTP non-2xx, SSRF block, generic throw) with latency + error message — provider->update(last_used_at) call removed since tracker handles it. |
| 8 | Silent bugs | 🟢 completed | TrendingAggregator recency boost rewritten with signed-safe `getTimestamp()` delta + Unit\Trending\TrendingAggregatorTest covering decay curve. SecurityAlertService::severity() now delegates to SecurityEvents::severity() (delete-and-delegate fix for the silent Slack/Discord black-hole) + Unit\Security\SecurityAlertServiceTest pins the contract. RewardsController stops writing to `user_levels.watch_streak` / `last_streak_date` (StreakService owns those columns via its legacy-mirror sync); 7-day reward cycle replaced with a flat daily check-in (DAILY_COIN_REWARD=10, DAILY_XP_REWARD=10); rewards view updated to match; double-claim race wrapped in try/catch. Honeypot now calls `AuditLogger::security(SecurityEvents::HONEYPOT_HIT, ...)` so the row is `is_security=true` and SecurityEventLogged fires. RequestFirewall swaps the raw `'security.waf.blocked'` / `'security.waf.ip_banned'` strings for `SecurityEvents::WAF_BLOCKED` / `WAF_IP_BANNED` constants. SetLocale runs the full priority chain on every request and only writes session('locale') when `?lang=XX` is provided (no more first-visit pin shadowing user.preferred_locale + Accept-Language). New migration `2026_05_10_240010_add_unique_index_to_watchlists` dedupes existing rows then re-adds the unique index defensively (idempotent across MySQL / SQLite / Postgres); WatchlistController::toggle now catches the duplicate-key QueryException so a tab-race no longer 500s. `quiz.submit` route gets `throttle:6,1` (6/min/IP) to deter brute-force answer probing. |
| 9 | X-Ray data + ops | 🟢 completed | SceneActorExtractor task (heuristic-first segment/cast round-robin with optional AI synopsis refinement; idempotent delete-then-insert; in-memory generator path for controller fallback); AiExtractSceneActors command `flik:ai:scene-actors {--all} {--movie=} {--limit} {--force}` accepting id-or-slug; weekly Sunday 02:00 cron `flik:ai:scene-actors --all` in Kernel; XrayController on-the-fly heuristic fallback (1h Cache::remember per movie) so any movie with cast renders hotspots immediately — eliminates the empty-polling UX bug. SendScheduleReminders scheduler entry (`->everyFiveMinutes()` with class_exists guard). WAF route_allowlist narrowed from blanket `admin.*` / `admin/*` to 6 specific code-bearing surfaces (pitch-deck, blog-posts, help-articles, feature-flags, settings, ai-providers) — `/admin/users`, `/admin/movies` CRUD etc. now get body+cookie inspection. New `flik:audit:prune --days=365 {--keep-security} {--dry}` command chunked at 1000 rows/txn, with per-iteration re-resolved query; weekly Sunday 04:00 schedule. UserDataExporter::scrubUser now includes phone/address/birth_date (via `makeVisible()` to bypass User::$hidden) + a descriptor object for national_id_hash (`{on_record, storage, note}`) so the user knows we hold the hash without dumping the raw digest. PII_PEPPER moved from `env('PII_PEPPER')` to `config('security.pii_pepper')` for config:cache safety; new `pii_pepper` key added to config/security.php with rotation procedure documented; Movie model gains `sceneActors()` hasMany relation. |
| 10 | PWA + tests | 🟢 completed | New `flik:pwa:generate-icons` Artisan command (PHP GD-only — no Intervention/ImageMagick dependency) renders the full PWA icon set from a source logo: 8 regular icons (72/96/128/144/152/192/384/512) + 2 maskable variants (192/512, 40% safe-zone padding so Android adaptive shapes don't clip) + 180px apple-touch-icon → all 11 PNGs landed in `public/icons/`. Existing `flik:pwa:generate-splash --force` re-run to ship 7 iOS splash screens (iPhone SE / 14 / 14 Pro / 14 Pro Max / iPad mini / iPad Pro 11 / iPad Pro 12). Synthesised gold-"F" placeholder when source missing + best-effort TTF font resolution (DejaVu/Liberation/Arial) so the icons still have a visible glyph on a fresh checkout. `<meta name="viewport">` in `layout.blade.php` now includes `viewport-fit=cover` (audit 19 M-1) so the installed PWA on iPhone X-class honours `env(safe-area-inset-*)`. `<x-mobile-nav />` refactored: grid-cols-5 → grid-cols-6 with a 6th "More" tab that opens an Alpine bottom sheet exposing the `<x-lang-switcher />` (one POST form per locale, `route('locale.switch')`, identical wiring) + Profile/Help/Logout links — closes audit 19 i18n-3 (mobile users finally have an in-app affordance to change locale). `PwaInstallTrackController` now peppers `ip_hash` with `APP_KEY` (`sha256($ip . '|' . config('app.key'))`) mirroring `ViewTracker::resolveIpHash()` — closes audit 19 PWA-8 (unpeppered hash trivially reversible from IPv4 keyspace). **41 feature tests added** across 7 files: `tests/Feature/AuthFlowTest.php` (5 — login happy / 2FA branch / register / logout / password reset no-enumeration), `tests/Feature/Payment/PaymentFlowTest.php` (4 — checkout snap, webhook settlement, promo validate, free-plan skip; skips when midtrans-php absent), `tests/Feature/Admin/AdminDashboardSmokeTest.php` (23 — every major /admin/* route as super_admin asserting NOT 401/403/5xx), `tests/Feature/Notifications/NotificationTest.php` (2 — AdminNotifier event fan-out + bell unread count JSON), `tests/Feature/Social/PublicProfileTest.php` (2 — public profile render + follow-button idempotent count), `tests/Feature/Trending/TrendingTest.php` (1 — TrendingAggregator end-to-end + Carbon-3 recency-boost regression guard), `tests/Feature/System/PwaAssetTest.php` (4 — manifest icons exist, apple-touch present, viewport-fit cover, IP-hash peppered). All test files lint clean (php -l). Match existing repo convention: RefreshDatabase + `markTestSkipped` on missing DB, super_admin-via-role-or-legacy-is_admin pattern from `AdminRouteGuardTest`. |

🟡 dispatched · 🟢 completed · 🔴 blocked · ⚠️ partial

---

## Expected outcome after swarm

- **0 release blockers** (all 500s fixed)
- **15 inactive features** become live
- **15 silent bugs** repaired
- **Test coverage** lifted from ~10% to ~30%
- **Satisfaction score** projected: 6.2 → 8.5
